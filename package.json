{
  "name": "iopn-defi-sentinel",
  "version": "1.0.0",
  "type": "module",
  "description": "DeFi Sentinel Vault - OPN Chain Testnet",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network opn_testnet",
    "verify": "npx hardhat verify --network opn_testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^3.0.18",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
